<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="MemberMapper">


<insert id="setWrite" parameterType="MemberVO">
	 insert into member values(#{id}, #{pw}, #{name}, #{phone}, #{email}, #{gender}, 2)
</insert>
 
 <delete id="setDelete" parameterType="java.util.List">
 	delete member where id = #{id}
 </delete>
 
	<resultMap type="MemberVO" id="member">
		<id property="id" column="id" />
		<result property="pw" column="pw"/>
		<result property="name" column="name" />
		<result property="email" column="email"/>
		<result property="gender" column="gender" />
		<result property="grade" column="grade"/>
 		<association property="memberFileVO" resultMap="fileResult" javaType="com.project.file.MemberFileVO" />
 	 
 </resultMap>
 <resultMap type="com.project.file.MemberFileVO" id="fileResult">
 	<id property="fnum"	column="fnum" />
 	<result property="fname" column="fname" />
 	<result property="oname" column="oname" />
 </resultMap>
 
 <select id="getSelect" resultMap="member" parameterType="MemberVO" resultType="MemberVO">
 	select M.*, F.*
 	from member M Full Join MemberFile F on (M.id=F.id)
 	where M.id=#{id} and M.pw=#{pw}

 </select>
 
 
 
 
 
 
 
 
 
 
 
 
 </mapper>