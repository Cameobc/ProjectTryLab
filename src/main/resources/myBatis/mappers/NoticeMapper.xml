<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  
<mapper namespace="NoticeMapper">
	<insert id="setWrite" parameterType="com.project.trylab.notice.NoticeVO">
		<selectKey keyProperty="num" order="BEFORE" resultType="java.lang.Integer">
			select qna_seq.nextval from dual
		</selectKey>
		insert into notice values(#{num}, #{title}, #{writer}, #{contents}, sysdate, 0)
	</insert>

	<select id="getList" parameterType="com.project.trylab.util.PageMaker" resultType="com.project.trylab.notice.NoticeVO">
		select * from
		(select rownum R, N.* from
		(select * from notice where
		<choose>
			<when test="kind == 1">
				title
			</when>
			<when test="kind == 2">
				writer
			</when>
			<otherwise>
				contents
			</otherwise>
		</choose>
		like '%'|| #{search} || '%'
		order by num desc) N)
		where R between #{startRow} and #{lastRow}
	</select>
	
	<select id="getTotalCount" resultType="java.lang.Integer" parameterType="com.project.trylab.util.PageMaker">
		select count(num) from notice where
		<choose>
			<when test="kind == 1">
				title
			</when>
			<when test="kind == 2">
				writer
			</when>
			<otherwise>
				contents
			</otherwise>
		</choose>
		like '%'|| #{search} || '%'		
	</select>
	
	
	

</mapper>